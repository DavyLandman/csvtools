name: ci

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: compile
      run: make
    - name: test
      run: make test

    env:
      # https://github.com/DavyLandman/csvtools/issues/20
      CFLAGS: -Wp,-D_FORTIFY_SOURCE=3
